# Copy this file to .env and fill in your values.
# .env is in .gitignore — never commit it.

DB_URL=jdbc:postgresql://localhost:5432/atlas
DB_USERNAME=roms_user
DB_PASSWORD=roms_pass

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GITHUB_TOKEN=       # GitHub personal access token — gives 5000 API requests/hour
